server {
    listen 80;
    server_name rossulbricht.fun;
    server_tokens off:

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }

}

server {
    listen  443  ssl http2;
    server_name rossulbricht.fun;
    server_tokens off;

    ssl_certificate /etc/letsencrypt/live/www.rossulbricht.fun/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/www.rossulbricht.fun/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://petclinic:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
