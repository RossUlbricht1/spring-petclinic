version: '3.8'
  nginx:
    image: nginx
      volumes:
        - ./nginx.conf:/etc/nginx/conf.d/nginx.conf
      ports:
        - 80:80
      networks:
        - spring-petclinic_default
      depends_on:
        - petclinic-app
