worker_processes auto;

events {}

http {
    server {
        listen 80;
        server_name ${NGINX_SERVER_NAME};

        location / {
            proxy_pass http://${NGINX_SERVER_NAME}:8080;
        }
    }
}
